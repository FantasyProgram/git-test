name: custom workflow
on: push
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: set up
        uses: actions/checkout@v3
    
      - name: Install Node 
        uses: actions/setup-node@v3
        
      - name: run command
        run: |
          echo "this"
          
          
        
